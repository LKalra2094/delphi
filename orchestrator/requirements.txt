# NOTE: delphi_common is installed from the repo path, NOT listed here.
#   pip install ./libs/delphi_common
# It brings openai, asyncpg, httpx, fastapi, pydantic as transitive deps.
uvicorn>=0.29
python-dotenv>=1.0
langgraph>=0.2.28
langchain-core>=0.3
langchain-openai>=0.2
